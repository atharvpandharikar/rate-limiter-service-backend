# rate-limiter-service-backend


# To Run the SErvice : 
Make sure Docker Desktop is installed on the Laptop!! As for Database MySQL Docker is Used!!

Step 1 : npm install
Step 2 : Docker Compose up -d
step 3 : npm run dev

To check the service, Go to route "http://localhost:5000/data"!!
